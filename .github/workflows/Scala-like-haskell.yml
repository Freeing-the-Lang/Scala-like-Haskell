name: "Scala-like-Haskell — 3OS Build + ProofLedger + Clean Release"

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: scala-like-haskell-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-3os:
    name: "Build — ${{ matrix.os }} (${{ github.ref_name }})"
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]

    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # ----------------------------------------------------------
      # Setup GHC
      # ----------------------------------------------------------
      - name: Setup GHC
        uses: haskell/actions/setup@v2
        with:
          ghc-version: '9.4.7'
          cabal-version: '3.10.1.0'

      # ----------------------------------------------------------
      # Build Interpreter (핵심: -i src)
      # ----------------------------------------------------------
      - name: Build Interpreter
        shell: bash
        run: |
          mkdir -p dist
          echo "[Build] Compiling src/Main.hs with module path -i src"
          ghc -i src -outputdir dist -o dist/slh src/Main.hs

      # ----------------------------------------------------------
      # Run Example
      # ----------------------------------------------------------
      - name: Run Example
        shell: bash
        run: |
          echo "[Run] Executing interpreter..."
          ./dist/slh || true

      # ----------------------------------------------------------
      # ProofLedger 생성
      # ----------------------------------------------------------
      - name: Generate ProofLedger
        shell: bash
        run: |
          mkdir -p proof dist
          OUT="proof/proofledger.txt"

          echo "Scala-like-Haskell ProofLedger" > $OUT
          echo "OS: ${{ runner.os }}" >> $OUT
          echo "Commit: $GITHUB_SHA" >> $OUT
          echo "Timestamp: $(date)" >> $OUT
          echo "" >> $OUT
          echo "[SHA256 Checksums]" >> $OUT

          if command -v sha256sum >/dev/null 2>&1; then
            HASH="sha256sum"
          else
            HASH="shasum -a 256"
          fi

          $HASH dist/slh >> $OUT 2>/dev/null || true
          $HASH examples/hello.slh >> $OUT 2>/dev/null || true
          $HASH src/*.hs >> $OUT 2>/dev/null || true

      # ----------------------------------------------------------
      # Artifact Upload
      # ----------------------------------------------------------
      - name: Upload 3OS Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: scala-like-haskell-${{ runner.os }}
          path: |
            dist/**
            proof/**
            src/**
            examples/**

  # ==============================================================
  # Release Job — ZIP 하나만 업로드 (충돌/404 0%)
  # ==============================================================
  release:
    needs: build-3os
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: Create Tag
        id: tag
        run: |
          VERSION="v1.$(date +%Y%m%d%H%M%S)"
          git tag $VERSION
          git push origin $VERSION
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Download All Artifacts
        uses: actions/download-artifact@v4
        with:
          path: release

      # ----------------------------------------------------------
      # ZIP 하나로 통합 → 404/충돌 0%
      # ----------------------------------------------------------
      - name: Create Unified ZIP
        run: |
          cd release
          zip -r scala-like-haskell-${{ steps.tag.outputs.version }}.zip ./*

      - name: Publish Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ steps.tag.outputs.version }}
          name: "Scala-like-Haskell Release ${{ steps.tag.outputs.version }}"
          body: |
            ## Scala-like-Haskell Auto Release
            ✔ Haskell(GHC) 3OS 빌드 완료  
            ✔ ProofLedger 자동 생성  
            ✔ 모든 산출물 ZIP 하나에 통합  
            ✔ Release 충돌 및 404 완전 제거  
          files: |
            release/scala-like-haskell-${{ steps.tag.outputs.version }}.zip
